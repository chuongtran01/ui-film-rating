import Settings from "@/components/settings/Settings";

const page = () => {
  return (
    <div className="flex flex-col w-full min-h-screen">
      <Settings />
    </div>
  );
};

export default page;
